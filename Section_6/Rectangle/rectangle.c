#include <stdio.h>

int main(){
  double height = 10;
  double width = 10;
  double area = height * width;
  double perimeter = 2 * (height + width);
  printf("Area: %.1f\n", area);
  printf("Perimeter: %.1f\n", perimeter);
}
